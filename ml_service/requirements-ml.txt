# Optional heavy deps for the REAL models (USE_REAL_MODELS=true).
# Install on top of requirements.txt only when you want Prompt-Guard + Presidio:
#   pip install -r requirements.txt -r requirements-ml.txt
#   python -m spacy download en_core_web_lg
# Prompt-Guard-86M is a gated HF model — accept its terms + set HF auth first.
transformers==4.48.0
torch==2.5.1
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.8.3
